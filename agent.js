class agent {
    constructor(x,y,fov) {
        let trailList = []; //list of where the agents has been, this is for drawing the dots behind agentss, if this feature is turned off, do not store to this list
        let left_weight = 0;    //left weight, will sum the values of all trails 
        let middle_weight = 0;
        let right_weight = 0;
    }
}